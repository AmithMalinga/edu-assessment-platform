{
  "name": "exam-master",
  "private": true,
  "packageManager": "npm@11.5.1",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "dev:full": "turbo run dev --filter=server --filter=web --filter=admin",
    "db:generate": "npm run db:generate --workspace=@repo/database",
    "lint": "turbo run lint",
    "clean": "turbo run clean",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "eslint-config-next": "^14.2.3",
    "next": "^14.2.3",
    "prettier": "latest",
    "turbo": "latest"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "prisma": "^5.22.0"
  }
}
